{
  "name": "drover-job-search",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight job search and tracking tool",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "crawl": "npm run cli -- crawl",
    "portal": "npm run dev -w apps/portal",
    "verify": "npm run verify -w packages/db -w packages/provider-linkedin -w packages/test-fixtures -w apps/portal -w apps/cli -w packages/providers -w packages/workers",
    "cli": "npm start -w apps/cli --"
  },
  "devDependencies": {
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "pino-pretty": "^13.1.3"
  }
}
