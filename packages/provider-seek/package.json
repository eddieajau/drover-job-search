{
  "name": "provider-seek",
  "version": "0.1.0",
  "private": true,
  "description": "Seek job page parser",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run --reporter=dot",
    "verify": "oxfmt check . && oxlint . && npm test"
  }
}
