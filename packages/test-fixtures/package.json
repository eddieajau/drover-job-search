{
  "name": "test-fixtures",
  "version": "0.1.0",
  "description": "Shared Fastify route-test harness and seed fixtures for Drover",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run --reporter=dot",
    "verify": "oxfmt check . && oxlint . && vitest run --reporter=dot"
  },
  "dependencies": {
    "db": "^0.1.0",
    "workers": "^0.1.0"
  },
  "devDependencies": {
    "@fastify/sensible": "^6.0.4",
    "@types/better-sqlite3": "^9.6.0",
    "better-sqlite3": "^13.0.3",
    "fastify": "^5.11.2",
    "vitest": "^3.2.7"
  }
}
