{
  "name": "provider-linkedin",
  "version": "0.1.0",
  "private": true,
  "description": "LinkedIn API provider",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "verify": "oxfmt check . && oxlint . && npm test"
  }
}
