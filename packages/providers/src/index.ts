/**
 * @copyright 2026 Andrew Eddie. All rights reserved.
 * @license   MIT
 */

// Top-level barrel for the `providers` package.
//
// This is intentionally minimal for now. The package is organised by subpath
// (`providers/common` today, and `providers/seek` / `providers/linkedin` as
// adapters land). Top-level re-exports are added as the provider adapters
// arrive.

export {}
