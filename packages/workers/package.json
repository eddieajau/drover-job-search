{
  "name": "workers",
  "version": "0.1.0",
  "private": true,
  "description": "Drover pipeline drain machinery (fetch-job-details, rank-job-details, worker loop)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run --reporter=dot",
    "verify": "oxfmt check . && oxlint . && vitest run --reporter=dot"
  },
  "dependencies": {
    "db": "^0.1.0",
    "drizzle-orm": "^0.45.2",
    "turndown": "^7.2.4"
  },
  "devDependencies": {
    "@types/turndown": "^5.0.6",
    "better-sqlite3": "^13.0.3",
    "pino": "^10.3.1",
    "vitest": "^3.2.7"
  }
}
